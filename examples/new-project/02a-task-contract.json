{
    "task_id":  "T-401",
    "title":  "Implement GET /health endpoint",
    "objective":  "Provide a health-check endpoint so load balancers and operators can verify the service process is up and responding.",
    "current_state":  "internal/health/ package does not exist yet. cmd/server/main.go already calls health.RegisterRoutes(mux) in anticipation of this package, so the binary will not build until this task lands.",
    "target_behavior":  "internal/health exports RegisterRoutes(mux *http.ServeMux), which registers a GET /health route. A GET request responds 200 with JSON body {\"status\":\"ok\"} and Content-Type: application/json. Any other HTTP method on /health responds 405.",
    "must_read_files":  [],
    "editable_files":  [
                           "internal/health/handler.go",
                           "internal/health/handler_test.go"
                       ],
    "forbidden_files":  [
                            "cmd/server/main.go",
                            "internal/config/config.go",
                            "internal/config/config_test.go"
                        ],
    "interfaces_to_preserve":  [
                                   "func RegisterRoutes(mux *http.ServeMux) signature, exactly as already called from cmd/server/main.go"
                               ],
    "input_format":  "HTTP GET request to /health, no body, no query parameters.",
    "output_format":  "HTTP 200 response, Content-Type: application/json, body exactly {\"status\":\"ok\"}.",
    "error_handling":  [
                           "Any HTTP method other than GET on /health must return 405 Method Not Allowed."
                       ],
    "preservation_conditions":  [

                                ],
    "completion_criteria":  [
                                "GET /health returns HTTP 200 with body {\"status\":\"ok\"} and Content-Type: application/json.",
                                "POST /health returns HTTP 405.",
                                "go test ./internal/health/... passes with zero failures."
                            ],
    "goal":  "Implement the bounded GET /health route and its tests without changing the server entrypoint, then return reproducible evidence.",
    "success_criteria":  [
                                "GET /health returns the required JSON 200 response and non-GET requests return 405.",
                                "The named Go test command passes and its output is reported verbatim."
                            ],
    "failure_criteria":  [
                                "The worker needs to edit a forbidden file or the health package tests fail after scoped fixes."
                            ],
    "termination_criteria":  [
                                "Stop after the route behavior and tests pass with the required evidence captured.",
                                "Stop and report if the parent contract requires changes outside the health package."
                            ],
    "required_evidence":  [
                                "Changed-file list, exact Go test command, and verbatim test output.",
                                "Direct evidence for GET /health and a non-GET 405 response."
                            ],
    "test_commands":  [
                          "go test ./internal/health/... -v"
                      ],
    "manual_verification":  [
                                "Once both T-401 and T-402 have landed and the binary builds, run the server and curl -i http://localhost:8080/health, confirming a 200 response with the expected JSON body."
                            ],
    "report_format":  "implementation-report.schema.json",
    "conflict_domains":  {
                             "files":  [
                                           "internal/health/handler.go",
                                           "internal/health/handler_test.go"
                                       ],
                             "data_structures":  [

                                                 ],
                             "interfaces":  [
                                                "internal/health.RegisterRoutes"
                                            ],
                             "db_entities":  [

                                             ],
                             "shared_configs":  [

                                                ],
                             "state_stores":  [

                                              ],
                             "build_targets":  [
                                                    "go test ./internal/health/..."
                                               ],
                             "user_flows":  [
                                                "GET /health request flow"
                                            ]
                         },
    "delegation":  {
                       "role":  "implementer",
                       "model":  "configured-worker-model",
                       "model_ceiling":  "configured-worker-model-ceiling",
                       "reasoning_effort":  "medium",
                       "execution":  "parallel",
                       "justification":  "T-401 is one of two independently verifiable work groups; its files, interfaces, data, generated output, shared state, and user flow are disjoint from T-402, the batch has no cross-group dependency edge, and planned_workers is bounded by observed native capacity.",
                       "spawn_authority":  "director"
                   }
}
